---
import { Icon } from 'astro-icon/components';
import Layout from '../layouts/Layout.astro';
---

<Layout title='Contacto - Acordes Gaditanos' description='Página de contacto de Acordes Gaditanos'>
  <div class='main-container container-layout'>
    <article class='contact-page'>
      <section class='contact-info' aria-labelledby='contact-heading'>
        <header>
          <h2 id='contact-heading'>Contacto</h2>
        </header>

        <div class='contact-methods'>
          <p class='intro-text'>
            Puedes contactarnos rellenando el siguiente formulario de contacto o, si lo prefieres,
            mediante <a href='mailto:acordesgaditanos@gmail.com'>correo electrónico</a> o <a
              href='https://twitter.com/acordesgaditano'
              target='_blank'
              rel='noopener'>Twitter</a
            >.
          </p>
          <p class='contact-method contact-method--email'>
            EMAIL: <a href='mailto:acordesgaditanos@gmail.com'>acordesgaditanos@gmail.com</a>
          </p>
          <p class='contact-method contact-method--twitter'>
            <Icon name='x' width={16} height={16} />
            <span>:</span>
            <a href='https://twitter.com/acordesgaditano' target='_blank' rel='noopener'>
              @acordesgaditano
            </a>
          </p>
        </div>
      </section>

      <section class='contact-form-section' aria-labelledby='form-heading'>
        <h3 id='form-heading' class='visually-hidden'>Formulario de contacto</h3>
        <form
          id='contact-form'
          method='post'
          novalidate
          data-endpoint='https://formspree.io/f/xdkjawaj'>
          <div class='form-group'>
            <label for='name' class='visually-hidden'>Nombre</label>
            <input
              name='name'
              id='name'
              class='form-control'
              placeholder='NOMBRE'
              aria-label='Nombre'
            />
            <div class='error' id='error-name' aria-hidden='true'>Rellena este campo</div>
          </div>

          <div class='form-group'>
            <label for='email' class='visually-hidden'>Email</label>
            <input
              name='email'
              id='email'
              type='email'
              class='form-control'
              placeholder='EMAIL'
              aria-label='Email'
            />
            <div class='error' id='error-email' aria-hidden='true'>Introduce un email válido</div>
          </div>

          <div class='form-group'>
            <label for='message' class='visually-hidden'>Mensaje</label>
            <textarea
              name='message'
              id='message'
              class='form-control'
              placeholder='MENSAJE'
              rows='1'
              aria-label='Mensaje'></textarea>
            <div class='error' id='error-message' aria-hidden='true'>Rellena este campo</div>
          </div>

          <div class='form-actions'>
            <button type='submit' class='btn btn--submit'>Enviar</button>
          </div>
        </form>
      </section>
    </article>
  </div>
</Layout>

<style>
  /* Global form elements */
  button,
  input,
  textarea,
  .main-container {
    font-family: 'Montserrat', sans-serif;
    font-weight: 700;
    letter-spacing: 1.4px;
  }

  /* Utility: visually hidden */
  .visually-hidden {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  /* Article container */
  .contact-page {
    position: relative;
    background: var(--card-background-color);
    border: 1px solid var(--primary-color);
    border-radius: 8px;
    padding: var(--card-padding);
    display: flex;
    gap: 32px;

    &::after {
      content: '';
      display: block;
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      border-radius: 8px;
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);
      z-index: -1;
    }
  }

  /* Contact info section (left) */
  .contact-info {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    gap: 16px;
    flex: 0 0 auto;

    & header {
      & h2 {
        position: relative;
        color: var(--primary-color);
        font-size: 1.625rem;
        margin: 0;

        &::after {
          content: '';
          display: block;
          position: absolute;
          left: 0;
          bottom: -10px;
          width: 140px;
          height: 4px;
          background: var(--primary-contrast-color);
        }
      }
    }
  }

  .contact-methods {
    display: flex;
    flex-direction: column;
    gap: 8px;
    font-size: 0.75rem;
    font-weight: 400;
    color: var(--text-color);
    line-height: 1.2rem;

    & a {
      color: inherit;
      text-decoration: none;
      transition: color 0.3s ease;

      &:hover {
        color: var(--link-color);
      }
    }

    & .intro-text {
      display: none;
    }

    & .contact-method {
      font-weight: 600;

      &--email {
        min-width: max-content;
      }

      &--twitter {
        display: flex;
        align-items: center;
        gap: 6px;
      }
    }
  }

  /* Form section (right) */
  .contact-form-section {
    flex: 1 1 auto;

    & form {
      display: flex;
      flex-direction: column;
      gap: 8px;
    }
  }

  .form-group {
    margin-bottom: 0;
  }

  .form-control {
    width: 100%;
    padding: 10px 0;
    background: none;
    border: none;
    border-bottom: 1px solid var(--placeholder-color);
    color: var(--primary-color);
    font-size: 0.875rem;
    outline: none;
    transition: border-color 0.2s;

    &::placeholder {
      color: var(--placeholder-color);
    }

    &:focus {
      border-bottom-color: var(--primary-contrast-color);
    }

    &.invalid {
      border-bottom-color: var(--error-color);
    }
  }

  .error {
    color: var(--error-color);
    font-size: 0.75rem;
    margin-top: 6px;
    min-height: 18px;
    visibility: hidden;
    opacity: 0;
    transition:
      opacity 160ms ease,
      visibility 160ms ease;

    &.active {
      visibility: visible;
      opacity: 1;
    }
  }

  .form-actions {
    text-align: right;
  }

  .btn {
    background: none;
    border: 1px solid var(--primary-color);
    border-radius: 8px;
    padding: 16px 24px;
    color: var(--primary-color);
    font-size: 0.875rem;
    cursor: pointer;
    outline: none;
    transition: all 0.5s ease;

    &:hover {
      border-color: var(--primary-contrast-color);
      transform: scale(1.05);
    }

    &.invalid {
      border-color: var(--error-color);
      color: var(--error-color);
      transform: none;
    }

    &.shake {
      animation: button-shake 420ms ease;
    }
  }

  @keyframes button-shake {
    0% {
      transform: translateX(0);
    }
    25% {
      transform: translateX(-6px);
    }
    50% {
      transform: translateX(6px);
    }
    75% {
      transform: translateX(-4px);
    }
    100% {
      transform: translateX(0);
    }
  }

  /* Media queries */
  @media screen and (max-width: 640px) {
    .contact-page {
      flex-direction: column;
      gap: 16px;
      padding: var(--card-padding-sm);
    }

    .contact-info {
      & header {
        & h2 {
          &::after {
            height: 3px;
            bottom: -3px;
          }
        }
      }
    }

    .contact-methods {
      font-size: 0.85rem;

      & .intro-text {
        display: block;
        font-size: 0.9rem;
        color: var(--primary-color);
      }

      & a {
        color: var(--link-color);
      }

      & .contact-method--email,
      & .contact-method--twitter {
        display: none;
      }
    }
  }
</style>
