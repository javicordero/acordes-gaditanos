---
import AutorCard from './AutorCard.astro';
const { autores, heading = 'Autores destacados' } = Astro.props;
const headingId = `autores-group`;
---

<article class='year-group' aria-labelledby={headingId}>
  <header>
    <h2 id={headingId} class='is-title-minus'>{heading}</h2>
  </header>

  <div class='acordes-grid' role='region' aria-labelledby={headingId}>
    {
      [...autores]
        .sort((a, b) => (a.data.order ?? 9999) - (b.data.order ?? 9999))
        .map((autor) => <AutorCard autor={autor.data} />)
    }
  </div>
</article>

<style>
  .year-group {
    position: relative;
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  header {
    & h2 {
      position: relative;
      color: var(--primary-color);
      font-size: 1.625rem;
      font-family: 'Montserrat', sans-serif;
      font-weight: 700;
      max-width: fit-content;
      margin: 0;

      &::after {
        content: '';
        display: block;
        position: absolute;
        left: 0;
        bottom: -4px;
        width: 90%;
        height: 4px;
        background: var(--primary-contrast-color);
      }
    }
  }

  .acordes-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--card-padding);
  }

  @media (max-width: 1024px) {
    .acordes-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  @media (max-width: 640px) {
    .year-group {
      gap: calc(var(--card-padding-sm) - 2px);
    }

    .acordes-grid {
      gap: 12px;
      grid-template-columns: repeat(2, 1fr);
      align-items: stretch;
    }

    header {
      & h2::after {
        height: 3px;
        bottom: -1px;
      }
    }
  }
</style>
